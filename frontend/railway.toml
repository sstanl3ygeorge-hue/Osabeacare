# Osabeacare Frontend - Railway Configuration

[build]
builder = "nixpacks"

[build.nixpacks]
buildCmd = "yarn install && yarn build"

[deploy]
startCommand = "npx serve -s build -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
